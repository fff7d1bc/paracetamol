diff --git a/rocm/ds4_rocm_matmul.cuh b/rocm/ds4_rocm_matmul.cuh
index d5049d7..0866758 100644
--- a/rocm/ds4_rocm_matmul.cuh
+++ b/rocm/ds4_rocm_matmul.cuh
@@ -547,7 +547,9 @@ static int cuda_matmul_q8_0_tensor_labeled(ds4_gpu_tensor *out, const void *mode
             return cuda_ok(cudaGetLastError(),
                            "matmul_q8_0 f32 tiny exact8 launch");
         }
-        if (!g_quality_mode && (in_dim % 32u) == 0u &&
+#if !defined(__HIP_DEVICE_COMPILE__) || defined(__gfx1150__) || defined(__gfx1151__)
+        if (g_gpu_device_major == 11 && !g_quality_mode &&
+            (in_dim % 32u) == 0u &&
             out_dim >= 1024u &&
             n_tok >= 256u &&
             in_dim <= UINT32_MAX && out_dim <= UINT32_MAX && n_tok <= UINT32_MAX) {
@@ -578,6 +580,7 @@ static int cuda_matmul_q8_0_tensor_labeled(ds4_gpu_tensor *out, const void *mode
                     blocks * 34u);
             return cuda_ok(cudaGetLastError(), "matmul_q8_0 f32 batch wmma 8w launch");
         }
+#endif
 #endif
         if ((in_dim & 31u) == 0u && out_dim <= UINT32_MAX && n_tok <= UINT32_MAX) {
             const uint32_t rows_per_block = 32u;
diff --git a/rocm/ds4_rocm_q8.cuh b/rocm/ds4_rocm_q8.cuh
index efb5ab0..57a2b58 100644
--- a/rocm/ds4_rocm_q8.cuh
+++ b/rocm/ds4_rocm_q8.cuh
@@ -617,6 +617,7 @@ __global__ static void matmul_q8_0_f32_batch_sharedx_exact8_kernel(
 typedef _Float16 __attribute__((ext_vector_type(16))) ds4_q8_half16_t;
 typedef float    __attribute__((ext_vector_type(8)))  ds4_q8_float8_t;
 
+#if !defined(__HIP_DEVICE_COMPILE__) || defined(__gfx1150__) || defined(__gfx1151__)
 /* Configurable wave-row Q8_0 batched GEMM experiment for gfx1151.
  * This is the hipfire/llama.cpp-style MMQ shape adapted to DS4's existing
  * F32 activation buffers: each block stages a 64-token x 32-K activation tile
@@ -727,7 +728,8 @@ __global__ static void matmul_q8_0_f32_batch_wmma_rowtile_kernel(
     }
 }
 
-#endif
+#endif // gfx11 WMMA
+#endif // HIP
 
 __global__ static void matmul_q8_0_pair_f32_warp8_kernel(
         float *out0,
diff --git a/rocm/ds4_rocm_runtime.cuh b/rocm/ds4_rocm_runtime.cuh
index 0d1ee54..a928154 100644
--- a/rocm/ds4_rocm_runtime.cuh
+++ b/rocm/ds4_rocm_runtime.cuh
@@ -17,6 +17,7 @@ static cudaEvent_t g_selected_readback_event;
 static uint64_t g_selected_readback_event_value;
 static cublasHandle_t g_cublas;
 static int g_cublas_ready;
+static int g_gpu_device_major;
 #ifdef __HIP_PLATFORM_AMD__
 static rocblas_handle g_rocblas;
 static int g_rocblas_ready;
@@ -5819,6 +5820,7 @@ extern "C" int ds4_gpu_init(void) {
     if (!cuda_ok(cudaSetDevice(dev), "set device")) return 0;
     cudaDeviceProp prop;
     if (cudaGetDeviceProperties(&prop, dev) == cudaSuccess) {
+        g_gpu_device_major = prop.major;
         fprintf(stderr, DS4_GPU_LOG_PREFIX "backend initialized on %s (sm_%d%d)\n",
                 prop.name, prop.major, prop.minor);
     }
